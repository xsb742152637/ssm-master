<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="model.core.guide.dao.CoreGuideFileDao">
    <!--表名-->
    <sql id="tableName">core_guide_file</sql>
    <!--主键-->
    <sql id="primaryKey">GUIDE_ID</sql>

    <select id="findOne" resultType="CoreGuideFileEntity">
        <include refid="GenericMapper.findOne"/>
    </select>

    <select id="findAll" resultType="CoreGuideFileEntity">
        <include refid="GenericMapper.findAll"/>
    </select>

    <insert id="insert" >
        <include refid="GenericMapper.insert"/>
    </insert>

    <update id="update">
        <include refid="GenericMapper.update"/>
    </update>

    <delete id="delete">
        <include refid="GenericMapper.delete"/>
    </delete>
    <delete id="deleteAll">
        <include refid="GenericMapper.deleteAll"/>
    </delete>
</mapper>

